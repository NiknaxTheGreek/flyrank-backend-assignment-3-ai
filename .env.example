# Copy to .env before a local Docker run and replace the development password.
POSTGRES_USER=flyrank
POSTGRES_PASSWORD=flyrank_dev_password
POSTGRES_DB=flyrank
API_PORT=8000
# DATABASE_URL is calculated by docker-compose.yml. Override it only when needed.